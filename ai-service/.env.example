# .env.example
# Copy to .env and fill in a strong random secret. Generate one with:
#   python -c "import secrets; print(secrets.token_hex(32))"
AI_SERVICE_INTERNAL_KEY=change-me-to-a-long-random-secret
PORT=8001
